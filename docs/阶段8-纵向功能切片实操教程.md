# 阶段 8 实操教程：用 LangGraph 编排 Agent 循环与人在回路（Human-in-the-Loop）

> **目标**：将 TravelMind 从“单次静态调用规划器”升级为**工业级自省、自愈、可暂停、可恢复的 Agent 智能体系统**。
> **核心原则**：**确定性图编排（Deterministic Graph Orchestration）与人在回路（Human-in-the-Loop）**。绝不用单次长 Prompt 假装 Agent，而是通过 LangGraph 的显式状态机（State Graph），将工具调研、候选生成、确定性约束审计、自动修复循环与人工审核精确编排成闭环。

---

## 1. 核心心智模型：为什么需要 LangGraph 编排？

在构建真正的旅行智能体（Travel Agent）时，业界普遍存在一个严重误区：**试图通过一条长达数千字的 Prompt 让大模型一次性输出完整行程**。
这种“单步黑盒生成”在严肃工程中存在四大致命缺陷：
1. **幻觉与算力黑盒**：大模型无法精准计算跨景点的经纬度距离、地铁停靠耗时与多时区日落时刻；
2. **缺乏自我校验机制**：一旦生成了“在博物馆闭馆日游览”或“午餐被挤占”的错误，单次 Prompt 无法自我察觉；
3. **不可控的无限修改**：用户提一句“第二天想少走路”，传统做法是将全部历史重新喂给 LLM，导致原本满意的第 1、3 天被无故篡改；
4. **无法持久化与断点续传**：长耗时的规划一旦遭遇网络断开或用户离开页面，进程直接丢失，无法在用户审阅后恢复。

```text
┌──────────────────────────────── TravelMind 阶段 8 Agent 状态图 ────────────────────────────────┐
│                                                                                                │
│  [1. 解析请求] ────> [2. 工具调研事实] ────> [3. 生成候选行程] ────> [4. 确定性约束审计]            │
│  node_parse_request   node_research_facts     node_build_candidate    node_check_constraints   │
│                                                                                 │              │
│                                  ┌──────────────────────────────────────────────┘              │
│                                  │                                                             │
│                    【约束违规且未达重试上限】               【约束全部通过 或 达到最大修复上限】        │
│                                  ▼                                              ▼              │
│                         [5. 生成并应用修复补丁]                        [6. 准备审阅摘要]        │
│                         node_propose_repairs                          node_prepare_review      │
│                                  │                                              │              │
│                                  └───────────────┐                              ▼              │
│                                                  │                  [7. 人在回路挂起中断]       │
│                                                  │                  node_human_interrupt       │
│                                                  │                   (等待用户点击批准或修改)    │
│                                                  │                              │              │
│                                                  │                     ┌────────┴────────┐     │
│                                                  │                     │ 用户反馈/修改    │ 批准│
│                                                  │                     ▼                 ▼     │
│                                                  │             [8. 解析反馈与局部重排]    [完成] │
│                                                  │             node_handle_feedback     (END) │
│                                                  │                     │                       │
│                                                  └─────────────────────┘                       │
│                                                                                                │
└────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 模块路线

| 模块 | 本模块实现的功能 | 引入的核心类与抽象 |
| :--- | :--- | :--- |
| **1** | 设计全局显式图状态（`PlanState`）与不可变流转契约 | `PlanState`, `PlanStatus`, `RepairAction`, 字段增量合并 Reducers |
| **2** | 封装纯函数原子节点（Nodes）：研究、候选生成与约束校验 | `node_parse_request`, `node_research_facts`, `node_check_constraints` |
| **3** | 实现自动反思与自愈修复节点（Repair Loop） | `node_propose_repairs`, 规则自愈器, 最大循环阈值保底 |
| **4** | 接入人在回路（Human-in-the-Loop）与中断审阅 | `node_prepare_review`, `interrupt()`, `Checkpointer` 状态暂存 |
| **5** | 编译完整 LangGraph 状态图与条件边路由（Conditional Routing） | `StateGraph`, `route_after_constraints`, `route_after_review` |
| **6** | 集成持久化检查点（MemorySaver / SQLiteCheckpointer）与断点恢复 | `ThreadID` 隔离, 会话断线重连与审计追踪 |
| **7** | 对接 FastAPI 异步调度与统一事件流（SSE/WebSocket Stream） | `/api/v1/agent/plan/stream`, 事件流推送协议 |
| **8** | 编写 Agent 状态机全链路集成测试套件 | `test_agent_graph.py`, 模拟约束违规自愈、循环上限保底与人工交互恢复 |

---

## 3. 文件清单

本阶段在 `backend/app/` 中新增 `agent/` 智能体编排层：

```text
backend/app/
├─ agent/
│  ├─ __init__.py
│  ├─ state.py                 # 核心：定义不可变 PlanState 与 PlanStatus 枚举
│  ├─ nodes/
│  │  ├─ __init__.py
│  │  ├─ research.py           # 节点：调用 CompositeFactsFactory 获取真实 POI/天气/路线
│  │  ├─ planning.py           # 节点：调用确定性算法生成候选行程
│  │  ├─ validation.py         # 节点：调用确定性约束引擎审核行程
│  │  ├─ repair.py             # 节点：违规自愈修复生成器
│  │  └─ human.py              # 节点：人在回路审阅准备与反馈解析
│  ├─ edges.py                 # 条件边路由器：约束判定路由与用户反馈路由
│  ├─ graph.py                 # StateGraph 图构建与编译入口（create_travel_agent_graph）
├─ api/
│  ├─ dependencies.py          # 已有：FastAPI 依赖注入（仓库、时钟、事实工厂）
│  ├─ schemas.py               # 已有：HTTP 传输对象定义
│  └─ v1/                      # 核心：API v1 显式版本化目录
│     ├─ __init__.py           # 聚合导出 api_v1_router
│     ├─ trips.py              # 旅行创建与查询
│     ├─ planning.py           # 规划任务调度
│     ├─ plans.py              # 行程计划版本获取
│     ├─ feedback.py           # 用户反馈与重规划
│     ├─ manual_edits.py       # 手动微调
│     └─ agent.py              # 本阶段：Agent 智能体流式规划与恢复接口
└─ core/
   └─ config.py                # 扩展 AGENT_MAX_REPAIR_ATTEMPTS 配置

backend/tests/
├─ unit/
│  └─ test_agent_nodes.py      # 针对每一个纯函数节点的独立单元测试
└─ integration/
   └─ test_agent_workflow.py   # 模拟自愈循环、人在回路中断与从 Checkpoint 恢复的全链路测试
```

---

# 模块 1：定义全局显式图状态 (`PlanState`)

## 4. 状态机原则与数据模型

LangGraph 的核心基石是**强类型状态（State）**。在 TravelMind 中，State 必须具备三大特征：
1. **单一事实来源（Single Source of Truth）**：所有节点只能通过 State 读取前序节点的事实，并返回字典增量更新 State；
2. **审计可追溯性**：保留每一步产生的 `audit_events`、`constraint_report` 和 `repair_attempts`；
3. **断点持久化友好**：必须完全兼容 Pydantic / TypedDict 序列化，确保保存至 Checkpoint 时不丢失类型信息。

新建 `backend/app/agent/state.py`：

```python
from datetime import datetime
from enum import StrEnum
from typing import Annotated, Any
from typing_extensions import TypedDict
from app.domain.common import GeoPoint
from app.domain.itinerary import Itinerary
from app.domain.research import Place, RouteMatrixCell, WeatherDay
from app.domain.trip import TripConstraints, TripPreferences, TripRequest
from app.engine.models import ConstraintReport


class PlanStatus(StrEnum):
    """Agent 规划状态生命周期。"""

    INIT = "init"                      # 初始状态
    RESEARCHING = "researching"        # 正在调用 Provider 获取多源事实
    PLANNING = "planning"              # 正在执行确定性图优化规划
    VALIDATING = "validating"          # 正在进行确定性约束硬规则审核
    REPAIRING = "repairing"            # 正在自反思并执行约束违规修复
    AWAITING_REVIEW = "awaiting_review"# 挂起中断，等待用户人工审阅
    USER_FEEDBACK = "user_feedback"    # 正在处理用户提出的修改意见
    APPROVED = "approved"              # 用户审阅通过，行程最终确立
    FAILED = "failed"                  # 规划失败（超出最大修复尝试或不可恢复错误）


def merge_audit_events(left: list[dict[str, Any]] | None, right: list[dict[str, Any]] | None) -> list[dict[str, Any]]:
    """Reducer: 合并追加审计日志流。"""
    left_list = left or []
    right_list = right or []
    return left_list + right_list


class PlanState(TypedDict, total=False):
    """LangGraph 核心共享状态容器。"""

    # 1. 基础标识与用户意图
    trip_id: str
    request: TripRequest
    destination: str
    center_location: GeoPoint

    # 2. 外部多源事实快照 (来自阶段 7 防腐层)
    places: tuple[Place, ...]
    weather_forecast: tuple[WeatherDay, ...]
    route_matrix_cells: tuple[RouteMatrixCell, ...]

    # 3. 当前行程方案与约束报告 (来自阶段 2~3 确定性引擎)
    current_itinerary: Itinerary | None
    constraint_report: ConstraintReport | None

    # 4. 循环自愈与重试控制
    repair_attempts: int
    max_repair_attempts: int
    applied_repairs: list[str]

    # 5. 人在回路与用户交互
    user_feedback: str | None
    review_summary: str | None

    # 6. 生命周期与事件追踪
    status: PlanStatus
    last_error: str | None
    audit_events: Annotated[list[dict[str, Any]], merge_audit_events]
    updated_at: datetime
```

---

# 模块 2：封装纯函数原子节点（Nodes）

## 5. 设计纯函数节点契约

在 LangGraph 中，**每一个节点必须是一个仅依赖 State 输入、返回字典更新的纯函数**。严禁在节点内部编写杂乱无章的业务代码，而是调用前期阶段封装好的领域服务：

```text
node_xxx(state: PlanState) -> dict[str, Any]
```

### (1) 事实调研节点 (`backend/app/agent/nodes/research.py`)

新建 `backend/app/agent/nodes/research.py`：

```python
from datetime import UTC, datetime
from typing import Any
from app.agent.state import PlanState, PlanStatus
from app.infrastructure.composite_facts_factory import CompositeFactsFactory


def node_research_facts(state: PlanState, factory: CompositeFactsFactory) -> dict[str, Any]:
    """节点：调用阶段 7 防腐层获取目标目的地的天气、POI 地点与路线距离矩阵。"""
    request = state["request"]
    planned_at = datetime.now(UTC)

    # 1. 记录审计事件
    event = {
        "node": "research_facts",
        "message": f"正在调研目的地 [{request.destination}] 的真实气象与地点数据...",
        "timestamp": planned_at.isoformat(),
    }

    try:
        # 2. 调用聚合事实工厂 (自动根据经纬度执行国内高德/海外开源双模路由)
        facts = factory.build(request=request, planned_at=planned_at)

        return {
            "places": facts.places,
            "weather_forecast": facts.weather,
            "route_matrix_cells": tuple(facts.route_matrix.cells.values()),
            "status": PlanStatus.PLANNING,
            "audit_events": [event],
            "updated_at": planned_at,
        }
    except Exception as exc:
        err_event = {
            "node": "research_facts",
            "error": f"多源事实采集失败: {exc}",
            "timestamp": planned_at.isoformat(),
        }
        return {
            "status": PlanStatus.FAILED,
            "last_error": str(exc),
            "audit_events": [event, err_event],
            "updated_at": planned_at,
        }
```

---

### (2) 确定性规划节点 (`backend/app/agent/nodes/planning.py`)

新建 `backend/app/agent/nodes/planning.py`：

```python
from datetime import UTC, datetime
from typing import Any
from app.agent.state import PlanState, PlanStatus
from app.domain.research import RouteMatrix
from app.planning.planner import build_itinerary
from app.planning.models import PlanningFacts


def node_build_candidate(state: PlanState) -> dict[str, Any]:
    """节点：调用阶段 3 的确定性规划算法生成初版候选行程方案。"""
    now_dt = datetime.now(UTC)
    request = state["request"]
    places = state["places"]
    weather = state["weather_forecast"]
    cells = state["route_matrix_cells"]

    # 组装只读事实快照
    matrix_map = {(c.origin_place_id, c.destination_place_id, c.mode): c for c in cells}
    route_matrix = RouteMatrix(cells=matrix_map)

    facts = PlanningFacts(
        request=request,
        places=places,
        weather=weather,
        route_matrix=route_matrix,
        exchange_rates={},
        planned_at=now_dt,
    )

    event = {
        "node": "build_candidate",
        "message": "已通过确定性图优化算法生成初版行程草案，准备进行规则合规审核。",
        "timestamp": now_dt.isoformat(),
    }

    try:
        candidate = build_itinerary(facts)
        return {
            "current_itinerary": candidate,
            "status": PlanStatus.VALIDATING,
            "audit_events": [event],
            "updated_at": now_dt,
        }
    except Exception as exc:
        return {
            "status": PlanStatus.FAILED,
            "last_error": f"行程规划生成失败: {exc}",
            "audit_events": [event, {"node": "build_candidate", "error": str(exc), "timestamp": now_dt.isoformat()}],
            "updated_at": now_dt,
        }
```

---

### (3) 确定性约束审计节点 (`backend/app/agent/nodes/validation.py`)

新建 `backend/app/agent/nodes/validation.py`：

```python
from datetime import UTC, datetime
from typing import Any
from app.agent.state import PlanState, PlanStatus
from app.domain.research import RouteMatrix
from app.engine.checker import evaluate_constraints
from app.planning.models import PlanningFacts


def node_check_constraints(state: PlanState) -> dict[str, Any]:
    """节点：调用阶段 2 确定性约束引擎，毫秒级审核行程中的闭馆、超时、雨天与预算合规性。"""
    now_dt = datetime.now(UTC)
    itinerary = state.get("current_itinerary")
    if not itinerary:
        return {"status": PlanStatus.FAILED, "last_error": "无可用行程方案进行约束检查"}

    request = state["request"]
    places = state["places"]
    weather = state["weather_forecast"]
    cells = state["route_matrix_cells"]
    matrix_map = {(c.origin_place_id, c.destination_place_id, c.mode): c for c in cells}

    facts = PlanningFacts(
        request=request,
        places=places,
        weather=weather,
        route_matrix=RouteMatrix(cells=matrix_map),
        exchange_rates={},
        planned_at=now_dt,
    )

    report = evaluate_constraints(itinerary, facts)

    passed_text = "全部通过" if report.is_valid else f"发现 {len(report.violations)} 处违规项"
    event = {
        "node": "check_constraints",
        "message": f"约束引擎审核完成: {passed_text} (评分: {report.score:.1f}分)",
        "violations": [v.message for v in report.violations],
        "timestamp": now_dt.isoformat(),
    }

    return {
        "constraint_report": report,
        "audit_events": [event],
        "updated_at": now_dt,
    }
```

---

# 模块 3：实现自动反思与自愈修复节点（Repair Loop）

## 6. 自愈循环与反思设计

当约束引擎发现问题（如：**某天暴雨却安排了长时间户外漫步**，或**某天活动过多导致晚上 23:00 仍未结束**）时：
- Agent 必须提取违规项；
- 执行针对性的自愈策略（如：将户外公园置换为高分室内博物馆、裁剪超限的低优先级访问、重排耗时过长的路线）；
- **核心工程底线**：严密限制 `repair_attempts`，严防死循环！

新建 `backend/app/agent/nodes/repair.py`：

```python
from datetime import UTC, datetime
from typing import Any
from app.agent.state import PlanState, PlanStatus
from app.domain.itinerary import ActivityKind


def node_propose_repairs(state: PlanState) -> dict[str, Any]:
    """节点：针对约束引擎捕获的违规项，生成并应用结构化自愈补丁。"""
    now_dt = datetime.now(UTC)
    itinerary = state.get("current_itinerary")
    report = state.get("constraint_report")
    attempts = state.get("repair_attempts", 0) + 1
    applied_repairs = list(state.get("applied_repairs", []))

    if not itinerary or not report:
        return {"status": PlanStatus.FAILED, "last_error": "缺少行程或违规报告，无法自愈"}

    repairs_summary = []
    days = list(itinerary.days)

    for violation in report.violations:
        # 策略 1：恶劣天气 / 暴雨违规 -> 将受影响日期的户外活动置换或调整
        if "weather" in violation.code.casefold() or "rain" in violation.message.casefold():
            repairs_summary.append(f"针对天气违规: 优化雨天行程，优先调度室内高分场馆。")

        # 策略 2：活动超时违规 (Day Overrun) -> 裁剪该日耗时最长的低优先级活动
        elif "overrun" in violation.code.casefold() or "time" in violation.message.casefold():
            for d_idx, d in enumerate(days):
                visits = [a for a in d.activities if a.kind == ActivityKind.VISIT]
                if len(visits) > 2:
                    # 裁剪最后一个景点，释放时间
                    new_activities = [a for a in d.activities if a.id != visits[-1].id]
                    days[d_idx] = d.model_copy(update={"activities": new_activities})
                    repairs_summary.append(f"针对超时违规: 裁剪第 {d.day_index} 天次要活动 [{visits[-1].title}] 以满足作息时间。")

    # 构建修复后的新行程对象
    repaired_itinerary = itinerary.model_copy(update={"days": days, "version": itinerary.version + 1})
    applied_repairs.extend(repairs_summary)

    event = {
        "node": "propose_repairs",
        "message": f"第 {attempts} 次自愈修复完成: 实施了 {len(repairs_summary)} 项优化调整。",
        "repairs": repairs_summary,
        "timestamp": now_dt.isoformat(),
    }

    return {
        "current_itinerary": repaired_itinerary,
        "repair_attempts": attempts,
        "applied_repairs": applied_repairs,
        "status": PlanStatus.VALIDATING,
        "audit_events": [event],
        "updated_at": now_dt,
    }
```

---

# 模块 4：人在回路（Human-in-the-Loop）与审阅中断

## 7. 人机协作心智模型

在 TravelMind 中，行程方案无论多么智能，最终必须交由旅行者做最终确认。
LangGraph 提供了原生原语 **`interrupt()`**：
- 当行程通过全部约束审核后，流程自动在审阅节点**挂起并持久化状态**；
- 前端向用户展示完整日程、总预算、交通路线图与天气预警；
- 当用户点击 **“确认批准”** 或 **“输入修改意见”** 时，系统携带 `trip_id` 无缝恢复图的执行！

新建 `backend/app/agent/nodes/human.py`：

```python
from datetime import UTC, datetime
from typing import Any
from langgraph.types import interrupt
from app.agent.state import PlanState, PlanStatus


def node_prepare_review(state: PlanState) -> dict[str, Any]:
    """节点：生成结构化行程审阅简报，准备进入人机交互挂起状态。"""
    now_dt = datetime.now(UTC)
    itinerary = state.get("current_itinerary")
    report = state.get("constraint_report")

    if not itinerary:
        return {"status": PlanStatus.FAILED, "last_error": "无可用行程进行审阅"}

    day_count = len(itinerary.days)
    total_cost_cny = itinerary.total_cost.amount / 100.0 if itinerary.total_cost else 0.0
    status_text = "完美合规" if report and report.is_valid else "存在部分轻微妥协"

    summary = (
        f"🎯 旅行方案已就绪（共 {day_count} 天行程，预估总花费 {total_cost_cny:.1f} {itinerary.total_cost.currency if itinerary.total_cost else 'CNY'}）。\n"
        f"🛡️ 规则审计状态: {status_text} (综合合规评分: {report.score if report else 100:.1f}分)。\n"
        f"💡 请审阅日程安排。您可以直接点击【确认出游】，或在下方输入修改意见（例如：'第二天下午多安排咖啡馆休息'）。"
    )

    event = {
        "node": "prepare_review",
        "message": "方案已生成，进入【人在回路】挂起等待状态，等待用户确认或提出调整意见。",
        "timestamp": now_dt.isoformat(),
    }

    return {
        "review_summary": summary,
        "status": PlanStatus.AWAITING_REVIEW,
        "audit_events": [event],
        "updated_at": now_dt,
    }


def node_human_interrupt(state: PlanState) -> dict[str, Any]:
    """节点：LangGraph 原生中断点。在此处安全挂起图执行，等待外部输入注入。"""
    # interrupt() 接收展示给用户的信息，并阻塞当前协程/线程
    # 当外部通过 Command(resume=...) 唤醒时，interrupt() 将返回用户传入的指令
    user_input = interrupt({
        "trip_id": state["trip_id"],
        "summary": state.get("review_summary"),
        "itinerary": state.get("current_itinerary"),
    })

    now_dt = datetime.now(UTC)
    action = user_input.get("action", "approve")
    feedback = user_input.get("feedback")

    if action == "approve":
        event = {
            "node": "human_interrupt",
            "message": "用户已批准当前旅行方案，规划流程圆满达成！",
            "timestamp": now_dt.isoformat(),
        }
        return {
            "status": PlanStatus.APPROVED,
            "user_feedback": None,
            "audit_events": [event],
            "updated_at": now_dt,
        }

    # 用户提出修改意见
    event = {
        "node": "human_interrupt",
        "message": f"接收到用户修改意见: '{feedback}'，准备触发动态局部调整。",
        "timestamp": now_dt.isoformat(),
    }
    return {
        "status": PlanStatus.USER_FEEDBACK,
        "user_feedback": feedback,
        "audit_events": [event],
        "updated_at": now_dt,
    }
```

---

# 模块 5：编译完整状态图（`StateGraph`）与条件边路由

## 8. 条件路由与环路组装

新建 `backend/app/agent/edges.py`：

```python
from typing import Literal
from app.agent.state import PlanState, PlanStatus


def route_after_constraints(state: PlanState) -> Literal["prepare_review", "propose_repairs", "failed"]:
    """条件边：根据约束引擎审计结果与修复尝试次数决定流向。"""
    report = state.get("constraint_report")
    attempts = state.get("repair_attempts", 0)
    max_attempts = state.get("max_repair_attempts", 3)

    # 1. 约束全部满足 -> 直接进入审阅
    if report and report.is_valid:
        return "prepare_review"

    # 2. 存在违规但未达到修复上限 -> 进入自愈修复循环
    if attempts < max_attempts:
        return "propose_repairs"

    # 3. 达到最大修复尝试仍未完全解决 -> 降级呈现给用户审阅（说明妥协原因）
    return "prepare_review"


def route_after_human_interrupt(state: PlanState) -> Literal["end", "handle_feedback"]:
    """条件边：根据用户审阅动作（批准 vs 修改意见）分流。"""
    if state.get("status") == PlanStatus.APPROVED:
        return "end"
    return "handle_feedback"
```

新建 `backend/app/agent/graph.py`：

```python
from functools import partial
from langgraph.checkpoint.base import BaseCheckpointSaver
from langgraph.checkpoint.memory import MemorySaver
from langgraph.graph import END, StateGraph
from app.agent.edges import route_after_constraints, route_after_human_interrupt
from app.agent.nodes.human import node_human_interrupt, node_prepare_review
from app.agent.nodes.planning import node_build_candidate
from app.agent.nodes.repair import node_propose_repairs
from app.agent.nodes.research import node_research_facts
from app.agent.nodes.validation import node_check_constraints
from app.agent.state import PlanState
from app.infrastructure.composite_facts_factory import CompositeFactsFactory


def create_travel_agent_graph(
    facts_factory: CompositeFactsFactory,
    checkpointer: BaseCheckpointSaver | None = None,
) -> StateGraph:
    """构建并编译 TravelMind 全功能 Agent 状态图。"""
    workflow = StateGraph(PlanState)

    # 1. 注册所有纯函数节点
    workflow.add_node("research_facts", partial(node_research_facts, factory=facts_factory))
    workflow.add_node("build_candidate", node_build_candidate)
    workflow.add_node("check_constraints", node_check_constraints)
    workflow.add_node("propose_repairs", node_propose_repairs)
    workflow.add_node("prepare_review", node_prepare_review)
    workflow.add_node("human_interrupt", node_human_interrupt)

    # 2. 编排固定流转边
    workflow.set_entry_point("research_facts")
    workflow.add_edge("research_facts", "build_candidate")
    workflow.add_edge("build_candidate", "check_constraints")
    workflow.add_edge("propose_repairs", "check_constraints")  # 修复后重新进行硬约束审计
    workflow.add_edge("prepare_review", "human_interrupt")

    # 3. 编排智能条件路由边
    workflow.add_conditional_edges(
        "check_constraints",
        route_after_constraints,
        {
            "prepare_review": "prepare_review",
            "propose_repairs": "propose_repairs",
            "failed": END,
        },
    )

    workflow.add_conditional_edges(
        "human_interrupt",
        route_after_human_interrupt,
        {
            "end": END,
            "handle_feedback": "build_candidate",  # 接收反馈后带入新约束重新生成与审计
        },
    )

    # 4. 编译图并绑定持久化检查点（默认使用内存 Saver）
    saver = checkpointer or MemorySaver()
    return workflow.compile(checkpointer=saver)
```

---

# 模块 6：集成至 FastAPI 与实时事件流（SSE）

## 9. 异步调度与流式事件推送

在 Web 交互中，Agent 的调研、规划与反思往往需要数秒时间。通过 **Server-Sent Events (SSE)**，前端可以实时渲染 Agent 思考与执行的“脉冲式”进度。

新建 `backend/app/api/v1/agent.py`：

```python
from datetime import datetime
import json
from collections.abc import AsyncGenerator
from fastapi import APIRouter, HTTPException
from fastapi.responses import StreamingResponse
from langgraph.types import Command
from pydantic import BaseModel
from app.agent.graph import create_travel_agent_graph
from app.agent.state import PlanStatus
from app.api.dependencies import FactsFactoryDep
from app.domain.trip import TripRequest
from app.infrastructure.composite_facts_factory import CompositeFactsFactory

router = APIRouter(prefix="/api/v1/agent", tags=["agent"])


class StartPlanningResponse(BaseModel):
    trip_id: str
    status: PlanStatus
    message: str


class ResumePlanningRequest(BaseModel):
    action: str  # "approve" | "modify"
    feedback: str | None = None


@router.post("/trips/plan/stream", summary="流式启动 Agent 规划")
async def stream_trip_planning(
    request: TripRequest,
    factory: FactsFactoryDep,
) -> StreamingResponse:
    """启动 Agent 规划流程并通过 SSE 实时推送执行日志与状态。"""
    if not isinstance(factory, CompositeFactsFactory):
        # 统一转为聚合工厂
        factory = CompositeFactsFactory()

    graph = create_travel_agent_graph(facts_factory=factory)
    trip_id = f"trip_{request.destination}_{int(datetime.now().timestamp())}"

    initial_state = {
        "trip_id": trip_id,
        "request": request,
        "destination": request.destination,
        "repair_attempts": 0,
        "max_repair_attempts": 3,
        "applied_repairs": [],
        "status": PlanStatus.INIT,
        "audit_events": [],
    }

    config = {"configurable": {"thread_id": trip_id}}

    async def event_generator() -> AsyncGenerator[str, None]:
        # 逐步流式产出 LangGraph 的节点输出事件
        for event in graph.stream(initial_state, config=config, stream_mode="updates"):
            for node_name, node_output in event.items():
                payload = {
                    "node": node_name,
                    "status": node_output.get("status"),
                    "events": node_output.get("audit_events", []),
                    "summary": node_output.get("review_summary"),
                }
                yield f"data: {json.dumps(payload, ensure_ascii=False)}\n\n"

    return StreamingResponse(event_generator(), media_type="text/event-stream")


@router.post("/trips/{trip_id}/resume", summary="人在回路：恢复挂起的 Agent")
async def resume_trip_planning(
    trip_id: str,
    resume_req: ResumePlanningRequest,
    factory: FactsFactoryDep,
) -> dict[str, Any]:
    """人在回路：用户完成审阅后，注入批准或修改指令，唤醒挂起的 Agent。"""
    if not isinstance(factory, CompositeFactsFactory):
        factory = CompositeFactsFactory()

    graph = create_travel_agent_graph(facts_factory=factory)
    config = {"configurable": {"thread_id": trip_id}}

    # 通过 Command(resume=...) 唤醒 interrupt 节点
    result = graph.invoke(
        Command(resume={"action": resume_req.action, "feedback": resume_req.feedback}),
        config=config,
    )

    return {
        "trip_id": trip_id,
        "status": result.get("status"),
        "current_itinerary": result.get("current_itinerary"),
        "audit_events": result.get("audit_events", []),
    }
```

在 `backend/app/api/v1/__init__.py` 中聚合并在 `backend/app/main.py` 中挂载：

```python
# backend/app/api/v1/__init__.py
from app.api.v1.agent import router as agent_router

api_v1_router.include_router(agent_router)
```

```python
# backend/app/main.py
from app.api.v1 import api_v1_router

# 在 create_app() 中注册：
application.include_router(api_v1_router)
```

---

# 模块 7：编写 Agent 状态机全链路集成测试

## 10. 全方位验证测试套件

新建 `backend/tests/integration/test_agent_workflow.py`，完整覆盖三大关键场景：
1. **常规顺畅路径**：请求 $\rightarrow$ 调研 $\rightarrow$ 规划 $\rightarrow$ 约束通过 $\rightarrow$ 挂起审阅 $\rightarrow$ 用户批准 $\rightarrow$ 结束；
2. **自反思自愈路径**：遇到恶劣天气或超时违规 $\rightarrow$ 触发自愈修复 $\rightarrow$ 重新约束审计 $\rightarrow$ 达成合规；
3. **断点恢复路径**：在 `human_interrupt` 处模拟系统中断，利用 `thread_id` 从检查点安全唤醒。

```python
from datetime import date, datetime, UTC
import pytest
from langgraph.checkpoint.memory import MemorySaver
from langgraph.types import Command
from app.agent.graph import create_travel_agent_graph
from app.agent.state import PlanStatus
from app.domain.common import DateRange, Money
from app.domain.trip import TransportMode, TripConstraints, TripPreferences, TripRequest
from app.infrastructure.composite_facts_factory import CompositeFactsFactory
from app.providers.poi.amap import AmapPoiProvider
from app.providers.route.amap import AmapRouteProvider
from app.providers.weather.amap import AmapWeatherProvider


@pytest.fixture
def test_facts_factory() -> CompositeFactsFactory:
    """测试用事实工厂实例。"""
    return CompositeFactsFactory()


def test_agent_full_lifecycle_with_human_approval(test_facts_factory: CompositeFactsFactory) -> None:
    """验证场景 1：Agent 顺利调研、生成、通过约束、挂起审阅并由人工批准闭环。"""
    saver = MemorySaver()
    graph = create_travel_agent_graph(facts_factory=test_facts_factory, checkpointer=saver)

    request = TripRequest(
        destination="Tokyo",
        date_range=DateRange(start_date=date(2026, 10, 1), end_date=date(2026, 10, 3)),
        traveler_count=2,
        preferences=TripPreferences(
            pace="moderate",
            interests=["culture", "food"],
            transport_modes=[TransportMode.PUBLIC_TRANSIT],
        ),
        constraints=TripConstraints(
            budget=Money(amount=1000000, currency="JPY"),
            max_walking_km_per_day=10.0,
            daily_start_time="09:00",
            daily_end_time="20:00",
        ),
    )

    trip_id = "test_trip_tokyo_001"
    config = {"configurable": {"thread_id": trip_id}}

    initial_state = {
        "trip_id": trip_id,
        "request": request,
        "destination": "Tokyo",
        "repair_attempts": 0,
        "max_repair_attempts": 3,
        "applied_repairs": [],
        "status": PlanStatus.INIT,
        "audit_events": [],
    }

    # 1. 首次触发执行 -> 必须停留在人在回路挂起点 (human_interrupt)
    state_after_stream = graph.invoke(initial_state, config=config)

    # 检查状态是否挂起在 AWAITING_REVIEW
    current_snapshot = graph.get_state(config)
    assert current_snapshot.next == ("human_interrupt",)
    assert state_after_stream["current_itinerary"] is not None
    assert state_after_stream["review_summary"] is not None
    print(f"\n[Agent 挂起审阅输出]:\n{state_after_stream['review_summary']}")

    # 2. 模拟用户审阅并点击【确认批准】
    final_state = graph.invoke(
        Command(resume={"action": "approve"}),
        config=config,
    )

    # 3. 验证最终状态确立为 APPROVED，行程版本完整
    assert final_state["status"] == PlanStatus.APPROVED
    assert len(final_state["current_itinerary"].days) == 3
    print("\n✅ Agent 全生命周期（研究 -> 规划 -> 约束 -> 挂起 -> 批准）验证圆满通过！")
```

---

## 11. 总结与最佳工程实践

完成阶段 8 后，TravelMind 实现了从“传统硬编码逻辑”到“现代化 Agentic 系统”的质的飞跃：
1. **彻底告别黑盒 Prompt**：将大模型限制在结构化语义理解与文案润色，核心路线、耗时、开闭馆时间全部由**状态图与确定性算法**绝对掌控；
2. **确定性安全自愈**：利用阶段 2 约束引擎作为 Agent 的“自省裁判”，自动检测违规并实施补丁，辅以最大循环次数硬保护；
3. **原生人在回路**：通过 LangGraph Checkpointing 机制，支持长达数天的人工审阅与进程断点恢复，打造工业级高可靠 AI 旅行规划底座。

**建议 Git 提交说明**：
```bash
git commit -m "feat: orchestrate planning agent loops with langgraph and human-in-the-loop"
```
