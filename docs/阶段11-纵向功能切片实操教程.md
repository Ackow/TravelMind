# 阶段 11 实操教程：将工具迁移为 MCP Server（Decoupling Travel Tools via Model Context Protocol）

> **目标**：将 TravelMind 中的外部事实数据提供者（天气、POI 地点、路线规划）通过 **Model Context Protocol (MCP)** 标准协议解耦为独立的微服务网关，实现工具发现、跨进程通信、权限隔离与防腐层无缝热切换。
> **核心原则**：**协议标准化与防腐隔离（Standard Protocol & Anti-Corruption Isolation）**。Agent 工作流与领域规划算法不直接依赖任何具体底层传输方式；系统支持在“进程内直接调用（In-Process）”与“标准 MCP 客户端远程调用（MCP Client via stdio / SSE）”之间零业务代码修改平滑切换。

---

## 1. 核心心智模型：为什么在工业级 Agent 中必须引入 MCP？

在阶段 7 中，我们通过防腐层适配器实现了外部 API（高德地图、和风天气、Overpass、OSRM）的接入。
虽然代码结构清晰，但在向企业级微服务或多 Agent 协作系统演进时，直接在主进程内硬编码工具函数会遇到三类架构瓶颈：

```text
❌ 传统进程内直接调用工具的三大痛点：
1. 【环境依赖与网络隔离冲突】：某些工具 Provider 依赖特殊的 C 底层库或特定的 Python 环境；在生产高可用集群中，外部爬虫/抓取工具常需要独立的网络代理通道与沙箱隔离，不能污染主业务服务。
2. 【多 Agent 无法共享工具网关】：当系统扩展出“行程助手”、“预算分析师”、“导游问答”等多个专属 Agent 时，每个 Agent 都要重复集成一套 Provider，工具更新或鉴权变更需要全量重新发布。
3. 【缺乏统一的工具发现与元数据契约】：每个工具的入参校验、错误结构、超时重试各行其是，LLM 容易因 Schema 歧义产生幻觉调用。
```

### 现代 MCP 工具网关架构：

```text
┌──────────────────────────────── TravelMind Agent 主服务 (Client) ────────────────────────────────┐
│                                                                                                  │
│   ┌───────────────────────────┐      ┌───────────────────────────┐      ┌─────────────────────┐  │
│   │ LangGraph 规划状态机      │      │ McpFactsFactory (防腐层)  │      │ McpClientAdapter    │  │
│   │ (StateGraph / Nodes)      │ ───> │ 统一转换为 PlanningFacts  │ ───> │ JSON-RPC 2.0 序列化 │  │
│   └───────────────────────────┘      └───────────────────────────┘      └──────────┬──────────┘  │
│                                                                                    │             │
└────────────────────────────────────────────────────────────────────────────────────┼─────────────┘
                                                                                     │
                                           ┌─────────────────────────────────────────┴─────────┐
                                           │ MCP Transport Layer (stdio 或 HTTP / SSE 远程传输)│
                                           └─────────────────────────────────────────┬─────────┘
                                                                                     │
┌──────────────────────────────── TravelMind MCP Server (独立微服务) ────────────────┼─────────────┐
│                                                                                    ▼             │
│   ┌───────────────────────────────────────────────────────────────────────────────────────────┐  │
│   │                                 FastMCP 工具注册与调度中枢                                │  │
│   │   • get_weather(dest, start, end)                  • search_poi(dest, query, cat, radius) │  │
│   │   • get_place_detail(dest, place_id)               • get_route(origin, dest, mode)        │  │
│   └───────────────────────────────────────────────┬───────────────────────────────────────────┘  │
│                                                   │                                              │
│                        ┌──────────────────────────┴──────────────────────────┐                   │
│                        ▼                                                     ▼                   │
│         【国内数据源 Provider】                                【海外数据源 Provider】           │
│          • AmapWeatherProvider (高德天气)                       • OpenMeteoWeather (开源气象)    │
│          • AmapPoiProvider (高德地点检索)                       • OverpassPoi (OSM 全球 POI)     │
│          • AmapRouteProvider (高德路径规划)                     • OSRMRouteProvider (全球路网)   │
│                                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 模块路线

| 模块 | 本模块实现的功能 | 引入的核心类与抽象 |
| :--- | :--- | :--- |
| **1** | 定义 MCP 工具契约与强类型 Pydantic Schema | `WeatherToolInput`, `PoiSearchInput`, `RouteToolInput`, `ToolResponse` |
| **2** | 构建独立 `travel-mcp-server` 服务 | `FastMCP`, `get_weather`, `search_poi`, `get_route`, `get_place_detail` |
| **3** | 编写后端 `McpClientAdapter` 客户端通信适配器 | `McpClientAdapter`, `AsyncExitStack`, `StdioServerParameters` |
| **4** | 实现 `McpFactsFactory` 防腐层与动态配置开关 | `McpFactsFactory`, `Settings.USE_MCP_TOOLS`, `Settings.MCP_SERVER_COMMAND` |
| **5** | 编写 MCP Server 契约测试与 Agent 端到端测试 | `test_mcp_server.py`, `test_mcp_facts_factory.py`, `test_agent_mcp_workflow.py` |

---

## 3. 文件清单

本阶段在根目录 `mcp_server/` 与 `backend/app/` 中新增与扩展的文件：

```text
TravelMind/
├─ mcp_server/                        # 本阶段新增：独立 MCP 工具服务根目录
│  ├─ pyproject.toml                  # MCP Server 依赖配置
│  ├─ server.py                       # FastMCP 服务入口与工具注册
│  ├─ schemas.py                      # MCP 协议标准入参/出参 DTO
│  └─ providers/                      # 复用或专用的 Provider 驱动
├─ backend/
│  ├─ app/
│  │  ├─ core/
│  │  │  └─ config.py                 # 扩展：USE_MCP_TOOLS, MCP_SERVER_HOST, MCP_SERVER_PORT 配置
│  │  ├─ infrastructure/
│  │  │  ├─ mcp_client.py             # 本阶段新增：MCP Client 底层连接管理
│  │  │  └─ mcp_facts_factory.py      # 本阶段新增：基于 MCP 的事实工厂防腐层实现
│  │  └─ main.py                      # 扩展：根据配置自动注入 McpFactsFactory
│  └─ tests/
│     ├─ unit/
│     │  └─ test_mcp_schemas.py       # 本阶段新增：Schema 校验测试
│     └─ integration/
│        ├─ test_mcp_server.py        # 本阶段新增：MCP 服务独立接口测试
│        └─ test_mcp_facts_factory.py # 本阶段新增：事实工厂对接测试
```

---

## 4. 模块 1：MCP 工具契约与强类型 Schema 设计

在 `mcp_server/schemas.py` 中定义所有 MCP 工具的标准输入输出格式，确保带有详尽的 JSON Schema 描述（LLM 可直接阅读）：

```python
# mcp_server/schemas.py
from datetime import date
from typing import Any, Literal
from pydantic import BaseModel, Field


class WeatherToolInput(BaseModel):
    """查询指定目的地在指定日期范围内的真实天气预报"""
    destination: str = Field(description="目标目的地城市名称，例如 'Tokyo' 或 '南京'")
    start_date: date = Field(description="行程开始自然日期，格式 YYYY-MM-DD")
    end_date: date = Field(description="行程结束自然日期，格式 YYYY-MM-DD")


class PoiSearchInput(BaseModel):
    """根据分类、关键词或辐射半径检索目的地的景点/美食/场馆点位"""
    destination: str = Field(description="目标城市，例如 'Tokyo'")
    query: str | None = Field(default=None, description="搜索关键词，例如 '浅草寺' 或 '拉面'")
    category: Literal["attraction", "restaurant", "cafe", "culture", "shopping", "all"] = Field(
        default="all",
        description="POI 分类类型",
    )
    center_lat: float | None = Field(default=None, description="中心点纬度（用于周边搜索）")
    center_lng: float | None = Field(default=None, description="中心点经度（用于周边搜索）")
    radius_meters: int = Field(default=10000, ge=500, le=50000, description="搜索半径（米）")
    limit: int = Field(default=20, ge=1, le=50, description="最大返回数量")


class RouteToolInput(BaseModel):
    """计算两点之间在指定交通方式下的真实通勤距离、耗时与预估费用"""
    origin_lat: float = Field(description="起点纬度")
    origin_lng: float = Field(description="起点经度")
    destination_lat: float = Field(description="终点纬度")
    destination_lng: float = Field(description="终点经度")
    transport_mode: Literal["walking", "transit", "driving", "cycling"] = Field(
        default="transit",
        description="交通出行方式",
    )


class ToolResponse(BaseModel):
    """统一工具响应外壳"""
    success: bool = Field(description="工具执行是否成功")
    data: Any = Field(default=None, description="成功返回的业务负载数据")
    error: str | None = Field(default=None, description="错误详情说明")
    provider_source: str = Field(description="实际响应的底层 Provider 标识")
```

---

## 5. 模块 2：构建独立 `travel-mcp-server` 服务

在 `mcp_server/server.py` 中使用官方 `mcp` SDK 或 `FastMCP` 实现服务中枢：

```python
# mcp_server/server.py
from datetime import date
from pathlib import Path
import sys

# 1. 自动校准 backend 寻址路径，确保独立运行、跨目录调用都不会出现导包错误 (ModuleNotFoundError)
BACKEND_DIR = Path(__file__).resolve().parent.parent / "backend"
if str(BACKEND_DIR) not in sys.path:
    sys.path.insert(0, str(BACKEND_DIR))

from mcp.server.mcpserver import MCPServer
from app.core.config import get_settings
from app.domain.common import GeoPoint
from app.domain.trip import TransportMode
from app.fixtures.loader import load_tokyo_places, load_tokyo_route_matrix, load_tokyo_weather
from app.infrastructure.composite_facts_factory import CompositeFactsFactory

server = MCPServer("TravelMind-Tool-Server")
settings = get_settings()


@server.tool(name="get_weather", description="获取目的地城市的逐日天气预报与气象灾害预警。")
def get_weather(destination: str, start_date: str, end_date: str) -> dict:
    """获取指定城市的天气列表（支持真实 API 与测试数据自适应）。"""
    try:
        s_date = date.fromisoformat(start_date)
        e_date = date.fromisoformat(end_date)

        if settings.DATA_PROVIDER_MODE == "live":
            factory = CompositeFactsFactory()
            weather_days = factory._weather_provider.get_forecast(destination, s_date, e_date)
            return {
                "success": True,
                "data": [d.model_dump(mode="json") for d in weather_days],
                "provider_source": "live_realtime_weather_api",
            }

        weather_days = load_tokyo_weather()
        return {
            "success": True,
            "data": [d.model_dump(mode="json") for d in weather_days],
            "provider_source": "tokyo_weather_fixture",
        }
    except Exception as exc:
        return {"success": False, "error": str(exc), "provider_source": "error"}


@server.tool(name="search_poi", description="根据地理区域、分类与关键词搜索候选景点与餐厅 POI。")
def search_poi(destination: str, query: str | None = None, category: str = "attraction", limit: int = 20) -> dict:
    """搜索目的地景点与生活设施点位（支持真实 API 与测试数据自适应）。"""
    try:
        if settings.DATA_PROVIDER_MODE == "live":
            factory = CompositeFactsFactory()
            places = factory._poi_provider.search_places(
                destination=destination,
                query=query or destination,
                categories=[category],
                limit=limit,
            )
            return {
                "success": True,
                "data": [p.model_dump(mode="json") for p in places],
                "provider_source": "live_realtime_poi_api",
            }

        places = load_tokyo_places()[:limit]
        return {
            "success": True,
            "data": [p.model_dump(mode="json") for p in places],
            "provider_source": "tokyo_poi_fixture",
        }
    except Exception as exc:
        return {"success": False, "error": str(exc), "provider_source": "error"}


@server.tool(name="get_route", description="计算两个地理坐标点之间的真实路线距离、通勤耗时与换乘方案。")
def get_route(
    origin_lat: float,
    origin_lng: float,
    dest_lat: float,
    dest_lng: float,
    mode: str = "transit",
) -> dict:
    """计算路径交通矩阵（支持真实 API 与测试数据自适应）。"""
    try:
        transport = TransportMode(mode)
        if settings.DATA_PROVIDER_MODE == "live":
            factory = CompositeFactsFactory()
            leg = factory._route_provider.calculate_leg(
                origin=GeoPoint(latitude=origin_lat, longitude=origin_lng),
                destination=GeoPoint(latitude=dest_lat, longitude=dest_lng),
                mode=transport,
            )
            return {
                "success": True,
                "data": leg.model_dump(mode="json"),
                "provider_source": "live_realtime_route_api",
            }

        matrix = load_tokyo_route_matrix()
        cell = matrix.cells[0] if matrix.cells else None
        return {
            "success": True,
            "data": {
                "distance_meters": cell.distance_meters if cell else 1500,
                "duration_minutes": cell.duration_minutes if cell else 15,
                "transport_mode": mode,
            },
            "provider_source": "tokyo_route_fixture",
        }
    except Exception as exc:
        return {"success": False, "error": str(exc), "provider_source": "error"}


if __name__ == "__main__":
    # 使用标准 stdio 方式启动
    server.run_stdio()
```

---

## 6. 模块 3：后端 `McpClientAdapter` 协议客户端

在 `backend/app/infrastructure/mcp_client.py` 中实现与 MCP Server 交互的异步客户端适配器：

```python
# backend/app/infrastructure/mcp_client.py
import asyncio
import json
import sys
from typing import Any
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client


class McpClientAdapter:
    """TravelMind MCP 客户端管理器，负责与独立 MCP 工具微服务建立通信。"""

    def __init__(self, command: str = sys.executable, args: list[str] | None = None) -> None:
        self._command = command
        self._args = args or ["-m", "mcp_server.server"]

    async def call_tool(self, tool_name: str, arguments: dict[str, Any]) -> dict[str, Any]:
        """通过 MCP 协议调用远程工具并解析返回结果。"""
        server_params = StdioServerParameters(
            command=self._command,
            args=self._args,
            env=None,
        )

        async with stdio_client(server_params) as (read, write):
            async with ClientSession(read, write) as session:
                # 1. 协议握手与初始化
                await session.initialize()

                # 2. 发起工具远程调用
                result = await session.call_tool(tool_name, arguments=arguments)

                # 3. 提取文本内容并反序列化 JSON
                if not result.content:
                    raise RuntimeError(f"MCP 工具 [{tool_name}] 返回空内容")

                raw_text = result.content[0].text
                response_json = json.loads(raw_text)
                if not response_json.get("success", False):
                    raise RuntimeError(f"MCP 工具 [{tool_name}] 执行失败: {response_json.get('error')}")

                return response_json["data"]

    def call_tool_sync(self, tool_name: str, arguments: dict[str, Any]) -> dict[str, Any]:
        """同步包装器，适配同步规划器与防腐层。"""
        return asyncio.run(self.call_tool(tool_name, arguments))
```

---

## 7. 模块 4：`McpFactsFactory` 防腐层实现与全局配置接入

在 `backend/app/infrastructure/mcp_facts_factory.py` 中实现 `FactsFactory` 协议：

```python
# backend/app/infrastructure/mcp_facts_factory.py
from datetime import datetime
from pydantic import TypeAdapter
from app.application.facts import FactsFactory
from app.domain.common import DataQuality, SourceRef
from app.domain.research import Place, RouteMatrix, WeatherDay
from app.domain.trip import TripRequest
from app.infrastructure.mcp_client import McpClientAdapter
from app.planning.models import PlanningFacts


class McpFactsFactory(FactsFactory):
    """基于 MCP 远程工具协议的规划事实工厂防腐层实现。"""

    def __init__(self, mcp_client: McpClientAdapter | None = None) -> None:
        self._client = mcp_client or McpClientAdapter()

    def build(self, request: TripRequest, planned_at: datetime) -> PlanningFacts:
        """通过调用 MCP 工具获取多源事实并封装为不可变领域对象。"""
        # 1. 调用 MCP 工具获取天气
        weather_raw = self._client.call_tool_sync(
            "get_weather",
            {
                "destination": request.destination,
                "start_date": request.date_range.start_date.isoformat(),
                "end_date": request.date_range.end_date.isoformat(),
            },
        )
        weather_days = TypeAdapter(list[WeatherDay]).validate_python(weather_raw)

        # 2. 调用 MCP 工具搜索地点 POI
        poi_raw = self._client.call_tool_sync(
            "search_poi",
            {
                "destination": request.destination,
                "query": request.destination,
                "category": "attraction",
                "limit": 25,
            },
        )
        places = TypeAdapter(list[Place]).validate_python(poi_raw)

        # 3. 构造路由矩阵
        source = SourceRef(
            provider="mcp-server",
            source_id="mcp-route-v1",
            fetched_at=planned_at,
            data_quality=DataQuality.VERIFIED,
        )
        route_matrix = RouteMatrix(cells=[], source=source)

        return PlanningFacts(
            request=request,
            places=tuple(places),
            weather=tuple(weather_days),
            route_matrix=route_matrix,
            exchange_rates={"JPY/CNY": 0.05, "CNY/JPY": 20.0, "USD/CNY": 7.2},
            planned_at=planned_at,
        )
```

在 `backend/app/core/config.py` 中增加配置：

```python
class Settings(BaseSettings):
    ...
    # MCP 工具网关配置 (阶段 11)
    USE_MCP_TOOLS: bool = False
    MCP_SERVER_COMMAND: str = "python"
    MCP_SERVER_ARGS: str = "-m mcp_server.server"
```

在 `backend/app/main.py` 中实现自动挂载：

```python
def get_default_facts_factory() -> FactsFactory:
    settings = get_settings()
    if settings.USE_MCP_TOOLS:
        from app.infrastructure.mcp_facts_factory import McpFactsFactory
        return McpFactsFactory()
    if settings.DATA_PROVIDER_MODE == "live":
        from app.infrastructure.composite_facts_factory import CompositeFactsFactory
        return CompositeFactsFactory()
    return TokyoFactsFactory()
```

---

## 8. 模块 5：MCP 契约与集成测试套件

在 `backend/tests/integration/test_mcp_facts_factory.py` 中编写集成验证：

```python
# backend/tests/integration/test_mcp_facts_factory.py
from datetime import datetime, timezone
import pytest
from app.fixtures.loader import load_tokyo_trip_request
from app.infrastructure.mcp_facts_factory import McpFactsFactory
from app.infrastructure.mcp_client import McpClientAdapter


def test_mcp_facts_factory_end_to_end():
    """验证：通过 MCP Client 跨进程调用 MCP Server 并正确生成领域 PlanningFacts。"""
    client = McpClientAdapter()
    factory = McpFactsFactory(mcp_client=client)
    request = load_tokyo_trip_request()
    now = datetime.now(timezone.utc)

    # 执行构建
    facts = factory.build(request=request, planned_at=now)

    # 验证领域对象完整性
    assert len(facts.weather) > 0
    assert len(facts.places) > 0
    assert facts.route_matrix is not None
    assert facts.exchange_rates.get("JPY/CNY") is not None
```

---

## 9. 阶段 11 总结与验收清单

- [x] **标准协议解耦（MCP Protocol）**：通过 Model Context Protocol 实现了工具层与主业务服务的彻底解耦；
- [x] **多语言与跨进程扩展**：外部 Provider 的依赖隔离在独立的 `mcp_server` 中，保障主后端极致纯粹与高可用；
- [x] **零业务侵入（Open-Closed Principle）**：Agent 规划状态机与约束引擎无需做任何改动，即可在进程内 Provider 与 MCP 远程网关间自由切换。
