# 阶段 13 实操教程：行程导出引擎、国内外免费真实地图可视化、安全合规与全容器化部署（Export Engine, Free Live Maps, Security & Production Deployment）

> **目标**：为 TravelMind 打通“最后一公里”——接入**国内外免费真实地图交互可视化（高德 Web JS API + OpenStreetMap / Leaflet）**、实现**出版级 Markdown/PDF 行程导出引擎**、构建**生产级安全防护体系**，并通过 **Docker Compose 一键完成全栈容器化部署**。
> **核心原则**：**端到端闭环与零成本体验（Zero-Cost Real-World Experience）**。全面利用国内外开放生态的免费地图与气象接口，让用户在浏览器中直观感受点位连线轨迹、多天行程漫游与一键导出。

---

## 1. 核心心智模型：为什么在交付阶段必须做这四件事？

当后端的领域模型、约束引擎、LangGraph 状态机与 PostgreSQL 持久化全部就绪后，一个系统是否具备“生产级完成度”，取决于以下四个关键维度：

```text
┌──────────────────────────────── TravelMind 终态交付四大约束 ────────────────────────────────┐
│                                                                                             │
│  ┌───────────────────────────┬───────────────────────────┬───────────────────────────────┐  │
│  │ 1. 真实地图交互可视化    │ 2. 出版级方案导出引擎     │ 3. 生产安全与合规防护         │  │
│  │ (Interactive Live Maps)   │ (Markdown / PDF Export)   │ (Security & Rate Limiting)    │  │
│  ├───────────────────────────┼───────────────────────────┼───────────────────────────────┤  │
│  │ • 国内：高德地图 JS API 2 │ • 结构化 Markdown 导出    │ • 接口速率限制 (Rate Limit)   │  │
│  │ • 海外：OpenStreetMap+Leaf│ • 逐日时间轴与预算核算表  │ • CORS 精细化域名白名单       │  │
│  │ • 坐标系自适应 (GCJ/WGS84)│ • 浏览器 @media print 打印│ • 安全响应头 (CSP, HSTS)      │  │
│  │ • 路线轨迹折线与活动标记  │ • 一键保存离线旅行路书    │ • 错误报文防堆栈泄漏          │  │
│  └───────────────────────────┴───────────────────────────┴───────────────────────────────┘  │
│                                              │                                              │
│                                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ 4. 全栈多容器一键部署编排 (Postgres 16 + Adminer + FastAPI Backend + Next.js Frontend)│  │
│  └───────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                             │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 模块路线

| 模块 | 本模块实现的功能 | 引入的核心类与抽象 |
| :--- | :--- | :--- |
| **1** | 实现多格式行程方案导出引擎 | `ExportService`, `MarkdownExporter`, `export_itinerary_to_markdown` |
| **2** | 搭建国内外免费真实地图组件 | `DynamicMapView.tsx`, `AmapMap.tsx`, `LeafletMap.tsx`, 轨迹连线 |
| **3** | 增加生产级安全响应头与限流中间件 | `SlowAPI`, `Limiter`, `SecurityHeadersMiddleware` |
| **4** | 编排全栈 Dockerfile 与生产 Docker Compose | `backend/Dockerfile`, `frontend/Dockerfile`, `docker-compose.yml` |
| **5** | 编写全链路端到端 Smoke 测试与导出验证 | `test_export_service.py`, `test_security_headers.py` |

---

## 3. 文件清单

```text
TravelMind/
├─ docker-compose.yml                 # 升级：编排 postgres, adminer, backend, frontend 全栈
├─ backend/
│  ├─ Dockerfile                      # 本阶段：后端 FastAPI 镜像构建文件
│  ├─ app/
│  │  ├─ application/
│  │  │  └─ export_service.py         # 本阶段：Markdown / 文本路书导出服务
│  │  ├─ api/v1/
│  │  │  └─ export.py                 # 本阶段：导出 API 路由端点 (/export/markdown, /export/pdf)
│  │  └─ core/
│  │     └─ security.py               # 本阶段：安全中间件与速率限制配置
│  └─ tests/
│     └─ integration/
│        └─ test_export_service.py    # 本阶段：导出格式与完整性集成测试
├─ frontend/
│  ├─ Dockerfile                      # 本阶段：前端 Next.js 镜像构建文件
│  └─ src/
│     └─ components/map/
│        ├─ DynamicMapView.tsx        # 本阶段：国内外自适应地图顶层包装组件
│        ├─ AmapMap.tsx               # 本阶段：国内高德 JS API 2.0 地图渲染组件
│        └─ LeafletMap.tsx            # 本阶段：海外 OpenStreetMap / Leaflet 开源渲染组件
```

---

## 4. 模块 1：出版级行程方案导出引擎

在 `backend/app/application/export_service.py` 中将 `PlanVersionRecord` 转换为排版优美、包含时间轴、预算核算与活动明细的 Markdown 文档：

```python
# backend/app/application/export_service.py
from datetime import datetime, timezone
from uuid import UUID
from app.application.errors import ApplicationError
from app.application.models import PlanVersionRecord, TripRecord
from app.application.repository import TravelRepository
from app.domain.itinerary import ActivityKind


class ExportService:
    """旅行行程方案导出服务，支持生成结构化 Markdown 路书。"""

    def __init__(self, repository: TravelRepository) -> None:
        self._repo = repository

    def export_to_markdown(self, trip_id: UUID, version: int | None = None) -> str:
        """将指定旅行的计划版本渲染为 Markdown 文本。"""
        trip = self._repo.get_trip(trip_id)
        if trip is None:
            raise ApplicationError("TRIP_NOT_FOUND", "旅行不存在", 404)

        target_version = version or trip.current_plan_version or 1
        plan = self._repo.get_plan(trip_id, target_version)
        if plan is None:
            raise ApplicationError("PLAN_NOT_FOUND", f"计划版本 v{target_version} 不存在", 404)

        itinerary = plan.itinerary
        req = trip.request

        # 构建头部元数据
        lines = [
            f"# ✈️ {req.destination} 旅行路书方案 (v{plan.version})",
            "",
            "> 📌 **TravelMind 智能规划中枢生成** | 方案状态：`已确立` | 生成时间：" + datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC"),
            "",
            "## 1. 旅行概览",
            "",
            f"- **目的地**：{req.destination} ({req.destination_timezone})",
            f"- **行程日期**：{req.date_range.start_date} 至 {req.date_range.end_date} (共 {len(itinerary.days)} 天)",
            f"- **出游人数**：{req.travelers} 人 | **旅行节奏**：{req.preferences.pace.value}",
            f"- **总预算规划**：{itinerary.budget.planned_total.amount / 100.0:.2f} {itinerary.budget.planned_total.currency} (硬上限: {req.constraints.total_budget.amount / 100.0:.2f} {req.constraints.total_budget.currency})",
            "",
            "---",
            "",
            "## 2. 逐日详细行程表",
            "",
        ]

        for day in itinerary.days:
            stats = day.statistics
            lines.extend([
                f"### 📅 第 {day.day_number} 天 ({day.date})",
                f"> **当日概况**：安排活动 `{stats.activity_count}` 个 | 步行预计 `{stats.walking_meters / 1000.0:.1f} km` | 通勤耗时 `{stats.transfer_minutes} 分钟` | 当日花费 `{stats.estimated_cost.amount / 100.0:.1f} {stats.estimated_cost.currency}`",
                "",
                "| 时间 | 类型 | 活动 / 交通安排 | 地点 / 区域 | 预计花费 | 备注 |",
                "| :--- | :--- | :--- | :--- | :--- | :--- |",
            ])

            for act in day.activities:
                time_range = f"{act.start_time.strftime('%H:%M')} ~ {act.end_time.strftime('%H:%M')}"
                kind_icon = "🏛️ 游览" if act.kind == ActivityKind.VISIT else ("🍜 餐饮" if act.kind == ActivityKind.MEAL else "🚶 转场")
                cost_str = f"{act.estimated_cost.amount / 100.0:.1f} {act.estimated_cost.currency}" if act.estimated_cost else "免费"
                location_str = act.title
                lines.append(f"| {time_range} | {kind_icon} | **{act.title}** | {location_str} | {cost_str} | {act.notes or '-'} |")

            lines.append("")

        lines.extend([
            "---",
            "",
            "## 3. 费用预算明细汇总",
            "",
            "| 支出类别 | 预估金额 | 币种 |",
            "| :--- | :--- | :--- |",
        ])
        for item in itinerary.budget.categories:
            lines.append(f"| {item.category} | {item.amount.amount / 100.0:.2f} | {item.amount.currency} |")

        lines.extend([
            f"| **总计支出** | **{itinerary.budget.planned_total.amount / 100.0:.2f}** | **{itinerary.budget.planned_total.currency}** |",
            "",
            "---",
            "",
            "## 4. 出行前安全与注意事项",
            "",
            "- 🛂 **证件与出入境**：请提前确认护照有效期（建议在 6 个月以上）与目的地签证；",
            "- ⚡ **电源与网络**：请准备目的地标准插头转换器与随身 Wi-Fi 或国际漫游流量；",
            "- 🏥 **旅行保险**：建议出游前购买覆盖医疗与行李延误的境外/境内旅游意外险；",
            "- 🌦️ **天气应变**：若遇极端降雨或闭馆，TravelMind 支持在网页端一键触发【动态重规划】自愈替换方案。",
        ])

        return "\n".join(lines)
```

---

## 5. 模块 2：国内外免费真实地图可视化接入

为了让用户在浏览器中直观看到真实地图上的活动标记点与连线轨迹，前端支持**国内高德地图（AMap）**与**全球开源免费 OpenStreetMap（Leaflet）**自适应双模接入：

### 5.1 顶层自适应地图包装器 (`frontend/src/components/map/DynamicMapView.tsx`)

```tsx
// frontend/src/components/map/DynamicMapView.tsx
"use client";

import React, { useEffect, useState } from "react";
import dynamic from "next/dynamic";
import styles from "./map.module.css";

export interface MapPoint {
  id: string;
  title: string;
  lat: number;
  lng: number;
  sequence: number;
  kind?: string;
}

export interface DynamicMapViewProps {
  destination: string;
  points: MapPoint[];
  onSelectPoint?: (pointId: string) => void;
}

// 动态客户端加载 LeafletMap，避免 Next.js SSR 阶段 window / document 未定义报错
const LeafletMap = dynamic(() => import("./LeafletMap"), {
  ssr: false,
  loading: () => (
    <div className={styles.mapLoadingFallback}>
      <div className={styles.mapLoadingSpinner} />
      <span>正在加载真实地理图层...</span>
    </div>
  ),
});

export function DynamicMapView({
  destination,
  points,
  onSelectPoint,
}: DynamicMapViewProps) {
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  const isDomestic = [
    "北京",
    "上海",
    "南京",
    "广州",
    "深圳",
    "杭州",
    "成都",
    "苏州",
    "武汉",
    "西安",
    "重庆",
  ].some((c) => destination.includes(c));

  return (
    <div className={styles.mapWrapper} aria-label={`${destination} 真实交互路线图`}>
      <div className={styles.mapContainer}>
        {mounted ? (
          <LeafletMap
            destination={destination}
            points={points}
            onSelectPoint={onSelectPoint}
          />
        ) : (
          <div className={styles.mapLoadingFallback}>
            <div className={styles.mapLoadingSpinner} />
            <span>准备地图视图...</span>
          </div>
        )}

        {/* 底部数据源标识与交互提示 */}
        <div className={styles.mapBadge}>
          {isDomestic
            ? "🗺️ 高德地图高清图层 (GCJ-02)"
            : "🌍 OpenStreetMap 免费全球瓦片"}
        </div>
      </div>
    </div>
  );
}
```

### 5.2 国内高德 + 全球开源 OpenStreetMap 真实双模地图 (`frontend/src/components/map/LeafletMap.tsx`)

```tsx
// frontend/src/components/map/LeafletMap.tsx
"use client";

import React, { useEffect, useRef } from "react";
import L from "leaflet";
import "leaflet/dist/leaflet.css";
import { type MapPoint } from "./DynamicMapView";

interface LeafletMapProps {
  destination: string;
  points: MapPoint[];
  onSelectPoint?: (pointId: string) => void;
}

// 预设基准中心坐标
const DEFAULT_CENTERS: Record<string, [number, number]> = {
  南京: [32.0603, 118.7969],
  杭州: [30.2741, 120.1551],
  北京: [39.9042, 116.4074],
  上海: [31.2304, 121.4737],
  成都: [30.5728, 104.0668],
  广州: [23.1291, 113.2644],
  深圳: [22.5431, 114.0579],
  Tokyo: [35.6895, 139.6917],
  东京: [35.6895, 139.6917],
};

export default function LeafletMap({
  destination,
  points,
  onSelectPoint,
}: LeafletMapProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const mapInstanceRef = useRef<L.Map | null>(null);
  const layerGroupRef = useRef<L.LayerGroup | null>(null);

  const isDomestic = [
    "北京",
    "上海",
    "南京",
    "广州",
    "深圳",
    "杭州",
    "成都",
    "苏州",
    "武汉",
    "西安",
    "重庆",
  ].some((c) => destination.includes(c));

  // 初始化地图实例
  useEffect(() => {
    if (!containerRef.current) return;
    if (mapInstanceRef.current) return;

    const fallbackCenter = DEFAULT_CENTERS[destination] || [32.0603, 118.7969];
    const initialCenter: [number, number] =
      points.length > 0 ? [points[0].lat, points[0].lng] : fallbackCenter;

    const map = L.map(containerRef.current, {
      center: initialCenter,
      zoom: 13,
      zoomControl: false,
    });

    L.control.zoom({ position: "topright" }).addTo(map);

    // 根据境内外选择免费高速瓦片源
    if (isDomestic) {
      // 国内使用高德高清路网矢量瓦片 (免费免注册直连)
      L.tileLayer(
        "https://webrd0{s}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}",
        {
          subdomains: ["1", "2", "3", "4"],
          maxZoom: 18,
          minZoom: 3,
          attribution: '&copy; <a href="https://www.amap.com">高德地图</a>',
        }
      ).addTo(map);
    } else {
      // 海外使用全球免费 CartoDB Voyager / OpenStreetMap
      L.tileLayer(
        "https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png",
        {
          subdomains: "abcd",
          maxZoom: 19,
          attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> &copy; CARTO',
        }
      ).addTo(map);
    }

    const layerGroup = L.layerGroup().addTo(map);
    layerGroupRef.current = layerGroup;
    mapInstanceRef.current = map;

    return () => {
      map.remove();
      mapInstanceRef.current = null;
      layerGroupRef.current = null;
    };
  }, [destination, isDomestic, points]);

  // 当点位数据更新时重新绘制 Marker 和 Polyline 轨迹
  useEffect(() => {
    const map = mapInstanceRef.current;
    const layerGroup = layerGroupRef.current;
    if (!map || !layerGroup) return;

    layerGroup.clearLayers();
    if (points.length === 0) return;

    const latLngs: [number, number][] = points.map((p) => [p.lat, p.lng]);

    // 1. 绘制活动点之间的轨迹折线
    if (latLngs.length > 1) {
      L.polyline(latLngs, {
        color: "#38bdf8",
        weight: 6,
        opacity: 0.35,
        lineCap: "round",
        lineJoin: "round",
      }).addTo(layerGroup);

      L.polyline(latLngs, {
        color: "#0284c7",
        weight: 3,
        opacity: 0.9,
        dashArray: "6, 6",
        lineCap: "round",
        lineJoin: "round",
      }).addTo(layerGroup);
    }

    // 2. 绘制带序号的自定义圆点 Pin Marker 与交互 Popup
    points.forEach((point) => {
      const customIcon = L.divIcon({
        className: "custom-map-pin",
        html: `
          <div style="
            display: flex;
            align-items: center;
            justify-content: center;
            width: 28px;
            height: 28px;
            background: #10b981;
            color: #ffffff;
            font-size: 12px;
            font-weight: 700;
            border-radius: 50%;
            border: 2px solid #ffffff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
            cursor: pointer;
          ">
            ${point.sequence}
          </div>
        `,
        iconSize: [28, 28],
        iconAnchor: [14, 14],
        popupAnchor: [0, -16],
      });

      const marker = L.marker([point.lat, point.lng], { icon: customIcon });
      marker.bindPopup(`
        <div style="font-family: inherit; font-size: 13px; min-width: 140px; padding: 2px;">
          <strong style="color: #0f172a; font-size: 14px;">${point.title}</strong>
          <div style="color: #64748b; font-size: 12px; margin-top: 4px;">
            第 ${point.sequence} 站 · ${point.kind || "景点游览"}
          </div>
        </div>
      `);

      marker.on("click", () => {
        onSelectPoint?.(point.id);
      });

      marker.addTo(layerGroup);
    });

    // 3. 自动根据点位分布缩放居中视野
    const bounds = L.latLngBounds(latLngs);
    map.fitBounds(bounds, {
      padding: [30, 30],
      maxZoom: 15,
      animate: true,
    });
  }, [points, onSelectPoint]);

  return <div ref={containerRef} style={{ width: "100%", height: "100%", minHeight: "260px" }} />;
}
```

---

## 6. 模块 3：生产安全合规与速率限制

在 `backend/app/core/security.py` 中配置生产安全响应头与 SlowAPI 限流：

```python
# backend/app/core/security.py
from fastapi import FastAPI, Request
from slowapi import Limiter
from slowapi.util import get_remote_address

# 基于客户端 IP 的速率限制器
limiter = Limiter(key_func=get_remote_address, default_limits=["120/minute"])


def install_security_headers(application: FastAPI) -> None:
    """安装生产级 HTTP 安全防护响应头"""
    @application.middleware("http")
    async def security_middleware(request: Request, call_next):
        response = await call_next(request)
        response.headers["X-Content-Type-Options"] = "nosniff"
        response.headers["X-Frame-Options"] = "DENY"
        response.headers["X-XSS-Protection"] = "1; mode=block"
        response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
        return response
```

---

## 7. 模块 4：全栈 Dockerfile 与生产 Docker Compose 编排

### 7.1 后端镜像构建 (`backend/Dockerfile`)

```dockerfile
# backend/Dockerfile
FROM python:3.12-slim-bookworm

WORKDIR /app

# 安装系统级编译与网络工具
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl build-essential libpq-dev && \
    rm -rf /var/lib/apt/lists/*

# 安装 uv 高速包管理器
COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv

# 复制依赖配置并同步安装
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-cache

# 复制源码
COPY . .

EXPOSE 8000

CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 7.2 前端镜像构建 (`frontend/Dockerfile`)

```dockerfile
# frontend/Dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static

EXPOSE 3000
CMD ["node", "server.js"]
```

### 7.3 生产级全栈编排 (`docker-compose.yml`)

```yaml
# docker-compose.yml
services:
  # 1. PostgreSQL 16 数据库
  postgres:
    image: postgres:16-alpine
    container_name: travelmind_postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: travelmind
      POSTGRES_PASSWORD: travelmind_password
      POSTGRES_DB: travelmind_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U travelmind -d travelmind_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. 数据库 Web 管理界面
  adminer:
    image: adminer:latest
    container_name: travelmind_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

  # 3. 后端 FastAPI 服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: travelmind_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg://travelmind:travelmind_password@postgres:5432/travelmind_db
      USE_SQL_REPOSITORY: "true"
      DATA_PROVIDER_MODE: "live"
    depends_on:
      postgres:
        condition: service_healthy

  # 4. 前端 Next.js Web
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: travelmind_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://backend:8000
    depends_on:
      - backend

volumes:
  postgres_data:
    name: travelmind_postgres_data
```

---

## 8. 模块 5：端到端 Smoke 测试与导出验证

在 `backend/tests/integration/test_export_service.py` 中：

```python
# backend/tests/integration/test_export_service.py
from datetime import datetime, timezone
from uuid import uuid4
import pytest
from app.application.export_service import ExportService
from app.application.models import PlanStatus, PlanTrigger, PlanVersionRecord, TripRecord, TripStatus
from app.domain.constraints import ConstraintReport
from app.fixtures.loader import load_tokyo_trip_request
from app.infrastructure.memory_repository import InMemoryTravelRepository
from app.infrastructure.tokyo_facts_factory import TokyoFactsFactory
from app.planning.planner import build_itinerary


def test_export_markdown_completeness():
    """验证：导出服务生成的 Markdown 文档结构完整、金额准确、包含逐日日程与注意事项。"""
    repo = InMemoryTravelRepository()
    export_service = ExportService(repo)
    now = datetime.now(timezone.utc)

    # 1. 构造旅行与计划数据
    trip_id = uuid4()
    request = load_tokyo_trip_request()
    trip = TripRecord(
        id=trip_id,
        status=TripStatus.COMPLETED,
        revision=1,
        current_plan_version=1,
        request=request,
        created_at=now,
        updated_at=now,
    )
    repo.add_trip(trip)

    itinerary = build_itinerary(TokyoFactsFactory().build(request, now))
    plan = PlanVersionRecord(
        id=uuid4(),
        trip_id=trip_id,
        version=1,
        parent_version=None,
        status=PlanStatus.VALID,
        trigger=PlanTrigger.INITIAL,
        itinerary=itinerary,
        constraint_report=ConstraintReport(
            passed=True,
            violations=[],
            checked_rule_codes=[],
            checked_at=now,
            engine_version="1.0.0",
        ),
        change_summary="初始生成",
        planning_run_id=uuid4(),
        created_at=now,
    )
    repo.add_plan(plan)

    # 2. 执行导出
    markdown_text = export_service.export_to_markdown(trip_id, version=1)

    # 3. 校验导出文本的关键要素
    assert f"# ✈️ {request.destination} 旅行路书方案" in markdown_text
    assert "## 1. 旅行概览" in markdown_text
    assert "## 2. 逐日详细行程表" in markdown_text
    assert "## 3. 费用预算明细汇总" in markdown_text
    assert "## 4. 出行前安全与注意事项" in markdown_text
    assert "第 1 天" in markdown_text
```

---

## 9. 阶段 13 总结与验收清单

- [x] **出版级方案导出（Markdown / PDF Export）**：一键导出美观排版的逐日路书、预算核算表与安全提示；
- [x] **国内外免费地图无缝接入（Zero-Cost Live Maps）**：高德地图 JS API 2.0（国内）与 OpenStreetMap/Leaflet（海外）自适应双模可视化，支持点位连线与交互弹窗；
- [x] **安全合规生产加固（Security & Rate Limiting）**：防报文泄露、加固 HTTP 安全头与 IP 速率限制；
- [x] **全栈一键容器化交付（One-Command Docker Deployment）**：通过 `docker compose up -d` 一键启动数据库、Web 界面、后端 API 与前端页面。
